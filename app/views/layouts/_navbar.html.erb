<nav class="navbar navbar-dark bg-custom-2">
  <div class="container-fluid">
    <a class="navbar-brand" href="#a">Schoolplus</a>
    <div class="logo"> 
      <%#= image_tag asset_url('logo.png') %>
    </div>
<!-- Login and Logout button -->

    <% if user_signed_in? %>
      Welcome <b><%= current_user.full_name %></b>, you are login with <b><%= current_user.role_name %></b> role<br>
      <% if current_user.school_admin? && @school.present? %>
        School Name: <b><%= @school.title.titleize %></b>
      <% end %>
      <%= button_to 'Log Out', destroy_user_session_path, method: :delete, class: 'btn btn-danger' %>
    <% elsif !user_signed_in? %>
      <%= link_to "Log In", new_user_session_path, method: :new, class: "btn btn-success" %>
    <% end %>
  </div>
</nav>