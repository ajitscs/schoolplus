<h1>Create new Batch for <b><%= @course.title.titleize %></b></h1>
<%= form_for @batch, url: school_course_batches_path(school_id: @school.id, course_id: @course), html: { class: "new_batch", id: "new_batch" } do |f| %>
  <%= render partial: "form", locals: { f: f } %>
<% end %>